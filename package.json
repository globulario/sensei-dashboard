{
  "name": "sensei-dashboard-contract",
  "private": true,
  "version": "0.0.0",
  "description": "Pinned dashboard-projection-v1 / agent-handoff-v1 contract tooling: schema mirror verification, generated TypeScript types, and fixture validation. Not the dashboard application itself.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "generate:types": "node scripts/generate-types.mjs",
    "verify:pin": "node scripts/verify-schema-pin.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1",
    "json-schema-to-typescript": "15.0.4",
    "typescript": "7.0.2"
  }
}
